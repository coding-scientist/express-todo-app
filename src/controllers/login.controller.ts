/* import {Router, Request, Response} from "express";

class LoginController {
  public getLoginPage(request: Request, response: Response): void
  {
    // Render the login page
  }

  public postLogin (request: Request, response: Response): Promise<void> 
  {
    // validate the received user object
    // check if provided password matches with database password
    // create a session and a session id
    // store the session information in the database
    // send back the session id as a cookie to the user
  }
}

export const loginRouter = Router();
const loginController = new LoginController();

loginRouter.get("/login", (request: Request, response: Response) => loginController.getLoginPage());
 */